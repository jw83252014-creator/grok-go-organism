# Working Memory Summary

The downloaded Gemini tab contains a long-running synthesis of Grok Go as a biological research organism.

## Core Ideas Preserved

- Grok Go is a persistent autonomous loop, not a one-shot chatbot.
- The biology mapping is central:
  - genome = directive files and memory;
  - metabolism = credits, compute, local models, revenue;
  - immune system = Watcher, guardrails, approval gates;
  - nervous system = Agent Bridge, Midspace, Researcher;
  - senses = logs, X, browser, source packets, telemetry;
  - digestion = Mining Engine and wiki/memory compression;
  - cancer/pathology = preening loops and self-referential polishing.
- Gemini is valuable for long-context synthesis, paper structure, NotebookLM prompts, and biological framing.
- Gemini itself cannot push GitHub or act locally unless wrapped by an external agent/tool surface.
- The Researcher layer should observe without contaminating the inner Grok Go run.
- The Mining Engine should be the low-metabolism foraging organ.
- X engagement can be a useful external fitness signal, but public actions need human approval.
- Librarian should own live X context and produce source packets for other agents.
- Midspace should be chat-first, phone-friendly, and approval-gated.

## Most Important Migration Point

The Gemini tab context becomes durable only after export and digestion.

Do not rely on the live tab as memory.

Use:

```text
source artifact -> summary -> memory pack -> receipts -> tasks
```

## Current Best Workflow

```text
Jeff/Sam drop link or idea
  -> Midspace routes it
  -> Librarian fetches source context
  -> Gemini Synthesis Cell connects it to the organism
  -> Codex/Grok Go builds or files
  -> Jeff approves public moves
```
